import { NavigationMenuDemo } from "./NavigationMenu";

interface IProps {}

const Header = ({}: IProps) => {
  return (
    <div>
      <NavigationMenuDemo />
    </div>
  );
};

export default Header;
