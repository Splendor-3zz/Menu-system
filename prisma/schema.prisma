datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  email String
  title String?
  role Role @default(USER)
  createdAt DateTime? @default(now())
  address Address?
  category Category[]
  item Item[]
}

type Address {
    city String
    street String
    building String
    apartment String
}

model Category {
  id     String @id @default(auto()) @map("_id") @db.ObjectId
  title  String
  imageUrl String
  userId String @db.ObjectId @default("68f55730cdad9b4fb1d95884")
  user   User   @relation(fields: [userId], references: [id])
  items  Item[]
  createdAt DateTime @default(now())
}

model Item{
    id        String   @id @default(auto()) @map("_id") @db.ObjectId
    title      String
    slug      String   @unique
    price     Float
    imageUrl String
    available Boolean  @default(true)
    user   User   @relation(fields: [userId], references: [id])
    userId String @db.ObjectId @default("68f55730cdad9b4fb1d95884")
    category  Category @relation(fields: [categoryId], references: [id])
    categoryId String  @db.ObjectId
    createdAt DateTime @default(now())
}

enum Role {
  USER
  ADMIN
}
